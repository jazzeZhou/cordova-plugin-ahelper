# cordova-plugin-Ahelper  
  
git init  
git add *  
git commit -m "first commit"  
git remote add origin https://github.com/jazzeZhou/  cordova-plugin-Ahelper.git  
git pull origin master  
git push -u origin master  
  
git add  
git commit -m "first commit"  
git push -u origin master  
  
  

#退出app功能（android）
window.plugins.Ahelper.exit();

